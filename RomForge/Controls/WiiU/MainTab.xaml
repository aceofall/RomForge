<UserControl x:Class="RomForge.Controls.WiiU.MainTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.expression/blend/2008" 
             xmlns:local="clr-namespace:RomForge.Controls.WiiU"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <TabControl x:Name="MasterTabControl" 
                    SelectedIndex="{Binding SubTabIndex, Mode=TwoWay}"
                    Background="Transparent" 
                    BorderThickness="0">

            <TabItem IsEnabled="{Binding IsIdle}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Assets/Images/Package.png" Width="20" Height="20" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="리팩" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <local:RepackTab DataContext="{Binding RepackVM}"/>
            </TabItem>
            
        </TabControl>
    </Grid>
</UserControl>